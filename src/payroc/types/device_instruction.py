# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .device_instruction_status import DeviceInstructionStatus
from .link import Link


class DeviceInstruction(UniversalBaseModel):
    """
    Object that contains information about the status of the instruction
    """

    status: typing.Optional[DeviceInstructionStatus] = pydantic.Field(default=None)
    """
    Indicates the current status of the instruction.  
    - `canceled` – The instruction was canceled before it was completed.
    - `completed` – The instruction has completed. Use the link object to check the resource.
    - `failure` – The instruction failed. Check the errorMessage field for more information.
    - `inProgress` – The instruction is currently in progress.
    """

    error_message: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="errorMessage"),
        pydantic.Field(
            alias="errorMessage",
            description="Description of the error that caused the instruction to fail.\n\n**Note:** We return this field only if the status is `failure`.",
        ),
    ] = None
    link: typing.Optional[Link] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
