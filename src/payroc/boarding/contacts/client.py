# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ...types.contact import Contact
from ...types.contact_method import ContactMethod
from ...types.contact_type import ContactType
from ...types.identifier import Identifier
from .raw_client import AsyncRawContactsClient, RawContactsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ContactsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawContactsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawContactsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawContactsClient
        """
        return self._raw_client

    def retrieve(self, contact_id: int, *, request_options: typing.Optional[RequestOptions] = None) -> Contact:
        """
        Use this method to retrieve details about a contact.

        To retrieve a contact, you need its contactId. Our gateway returned the contactId in the [Create Processing Account](https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account) method.

        **Note:** If you don't have the contactId, use the [List Contacts](https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts) method to search for the contact.

        Our gateway returns the following information about a contact:

        -    Name and contact method, including their phone number or mobile number.
        -    Role within the business, for example, if they are a manager.

        Parameters
        ----------
        contact_id : int
            Unique identifier for the contact.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Contact
            Successful request. Returns the requested contact.

        Examples
        --------
        from payroc import Payroc

        client = Payroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.boarding.contacts.retrieve(
            contact_id=1,
        )
        """
        _response = self._raw_client.retrieve(contact_id, request_options=request_options)
        return _response.data

    def update(
        self,
        contact_id_: int,
        *,
        type: ContactType,
        first_name: str,
        last_name: str,
        identifiers: typing.Sequence[Identifier],
        contact_methods: typing.Sequence[ContactMethod],
        contact_id: typing.Optional[int] = OMIT,
        middle_name: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Use this method to update a contact of a processing account.

        To update a contact, you need its contactId. Our gateway returned the contactId in the response of the [Create Processing Account](https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account) method.

        **Note:** If you don't have the contactId, use our [List Contacts](https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts) method to search for the contact.

        You can update the following details about a contact:

        -    First name and last name.
        -    Contact details, including their phone number or mobile number.
        -    Identification details, including their identification type and number.
        -    Role within the business, for example, if they are a manager.

        Parameters
        ----------
        contact_id_ : int
            Unique identifier for the contact.

        type : ContactType
            Type of contact.

        first_name : str
            Contact's first name.

        last_name : str
            Contact's last name.

        identifiers : typing.Sequence[Identifier]
            Array of identifier objects.

        contact_methods : typing.Sequence[ContactMethod]
            Array of contactMethod objects.
            **Note:** If you are adding information about an owner, you must provide at least an email address. If you are adding information about a contact, you must provide at least a contact number.

        contact_id : typing.Optional[int]
            Unique identifier of the contact.

        middle_name : typing.Optional[str]
            Contact's middle name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from payroc import ContactMethod_Email, Identifier, Payroc

        client = Payroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.boarding.contacts.update(
            contact_id_=1,
            type="manager",
            first_name="Jane",
            middle_name="Helen",
            last_name="Doe",
            identifiers=[
                Identifier(
                    type="nationalId",
                    value="000-00-4320",
                )
            ],
            contact_methods=[
                ContactMethod_Email(
                    value="jane.doe@example.com",
                )
            ],
        )
        """
        _response = self._raw_client.update(
            contact_id_,
            type=type,
            first_name=first_name,
            last_name=last_name,
            identifiers=identifiers,
            contact_methods=contact_methods,
            contact_id=contact_id,
            middle_name=middle_name,
            request_options=request_options,
        )
        return _response.data

    def delete(self, contact_id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Use this method to delete a contact associated with a processing account.

        To delete a contact, you need their contactId. Our gateway returned the contactId in the response of the [Create Processing Account](https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account) method.

        **Note:** If you don’t have the contactId, use our [Retrieve Processing Account](https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve) method or our [List Contacts](https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts) method to search for the contact.

        Parameters
        ----------
        contact_id : int
            Unique identifier for the contact.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from payroc import Payroc

        client = Payroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.boarding.contacts.delete(
            contact_id=1,
        )
        """
        _response = self._raw_client.delete(contact_id, request_options=request_options)
        return _response.data


class AsyncContactsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawContactsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawContactsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawContactsClient
        """
        return self._raw_client

    async def retrieve(self, contact_id: int, *, request_options: typing.Optional[RequestOptions] = None) -> Contact:
        """
        Use this method to retrieve details about a contact.

        To retrieve a contact, you need its contactId. Our gateway returned the contactId in the [Create Processing Account](https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account) method.

        **Note:** If you don't have the contactId, use the [List Contacts](https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts) method to search for the contact.

        Our gateway returns the following information about a contact:

        -    Name and contact method, including their phone number or mobile number.
        -    Role within the business, for example, if they are a manager.

        Parameters
        ----------
        contact_id : int
            Unique identifier for the contact.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Contact
            Successful request. Returns the requested contact.

        Examples
        --------
        import asyncio

        from payroc import AsyncPayroc

        client = AsyncPayroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.boarding.contacts.retrieve(
                contact_id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(contact_id, request_options=request_options)
        return _response.data

    async def update(
        self,
        contact_id_: int,
        *,
        type: ContactType,
        first_name: str,
        last_name: str,
        identifiers: typing.Sequence[Identifier],
        contact_methods: typing.Sequence[ContactMethod],
        contact_id: typing.Optional[int] = OMIT,
        middle_name: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Use this method to update a contact of a processing account.

        To update a contact, you need its contactId. Our gateway returned the contactId in the response of the [Create Processing Account](https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account) method.

        **Note:** If you don't have the contactId, use our [List Contacts](https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts) method to search for the contact.

        You can update the following details about a contact:

        -    First name and last name.
        -    Contact details, including their phone number or mobile number.
        -    Identification details, including their identification type and number.
        -    Role within the business, for example, if they are a manager.

        Parameters
        ----------
        contact_id_ : int
            Unique identifier for the contact.

        type : ContactType
            Type of contact.

        first_name : str
            Contact's first name.

        last_name : str
            Contact's last name.

        identifiers : typing.Sequence[Identifier]
            Array of identifier objects.

        contact_methods : typing.Sequence[ContactMethod]
            Array of contactMethod objects.
            **Note:** If you are adding information about an owner, you must provide at least an email address. If you are adding information about a contact, you must provide at least a contact number.

        contact_id : typing.Optional[int]
            Unique identifier of the contact.

        middle_name : typing.Optional[str]
            Contact's middle name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from payroc import AsyncPayroc, ContactMethod_Email, Identifier

        client = AsyncPayroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.boarding.contacts.update(
                contact_id_=1,
                type="manager",
                first_name="Jane",
                middle_name="Helen",
                last_name="Doe",
                identifiers=[
                    Identifier(
                        type="nationalId",
                        value="000-00-4320",
                    )
                ],
                contact_methods=[
                    ContactMethod_Email(
                        value="jane.doe@example.com",
                    )
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            contact_id_,
            type=type,
            first_name=first_name,
            last_name=last_name,
            identifiers=identifiers,
            contact_methods=contact_methods,
            contact_id=contact_id,
            middle_name=middle_name,
            request_options=request_options,
        )
        return _response.data

    async def delete(self, contact_id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Use this method to delete a contact associated with a processing account.

        To delete a contact, you need their contactId. Our gateway returned the contactId in the response of the [Create Processing Account](https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account) method.

        **Note:** If you don’t have the contactId, use our [Retrieve Processing Account](https://docs.payroc.com/api/schema/boarding/processing-accounts/retrieve) method or our [List Contacts](https://docs.payroc.com/api/schema/boarding/processing-accounts/list-contacts) method to search for the contact.

        Parameters
        ----------
        contact_id : int
            Unique identifier for the contact.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from payroc import AsyncPayroc

        client = AsyncPayroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.boarding.contacts.delete(
                contact_id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(contact_id, request_options=request_options)
        return _response.data
