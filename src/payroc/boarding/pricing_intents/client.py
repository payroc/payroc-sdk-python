# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.custom_pagination import AsyncPayrocPager, SyncPayrocPager
from ...core.request_options import RequestOptions
from ...types.base_intent_status import BaseIntentStatus
from ...types.base_us import BaseUs
from ...types.gateway_us_50 import GatewayUs50
from ...types.paginated_pricing_intent import PaginatedPricingIntent
from ...types.patch_request import PatchRequest
from ...types.pricing_agreement_us_50_country import PricingAgreementUs50Country
from ...types.pricing_agreement_us_50_processor import PricingAgreementUs50Processor
from ...types.pricing_agreement_us_50_version import PricingAgreementUs50Version
from ...types.pricing_intent import PricingIntent
from ...types.services_us_50 import ServicesUs50
from .raw_client import AsyncRawPricingIntentsClient, RawPricingIntentsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PricingIntentsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPricingIntentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPricingIntentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPricingIntentsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        before: typing.Optional[str] = None,
        after: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPayrocPager[PricingIntent, PaginatedPricingIntent]:
        """
        Use this method to return a [paginated](https://docs.payroc.com/api/pagination) list of pricing intents associated with the ISV.

        **Note:** If you want to view the details of a specific pricing intent and you have its pricingIntentId, use our [Retrieve Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/retrieve) method.

        Our gateway returns the following information about each pricing intent in the list:

        - Information about the fees, including the base fees, gateway fees, and processor fees.
        - Status of the pricing intent, including whether we approved the pricing intent.

        For each pricing intent, we also return its pricingIntentId which you can use to perform follow-on actions.

        Parameters
        ----------
        before : typing.Optional[str]
            Return the previous page of results before the value that you specify.

            You can’t send the before parameter in the same request as the after parameter.

        after : typing.Optional[str]
            Return the next page of results after the value that you specify.

            You can’t send the after parameter in the same request as the before parameter.

        limit : typing.Optional[int]
            Limit the maximum number of results that we return for each page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPayrocPager[PricingIntent, PaginatedPricingIntent]
            Successful response. Returns a list of pricing intents.

        Examples
        --------
        from payroc import Payroc

        client = Payroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        response = client.boarding.pricing_intents.list(
            before="2571",
            after="8516",
            limit=1,
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(before=before, after=after, limit=limit, request_options=request_options)

    def create(
        self,
        *,
        idempotency_key: str,
        key: str,
        country: PricingAgreementUs50Country,
        version: PricingAgreementUs50Version,
        base: BaseUs,
        id: typing.Optional[str] = OMIT,
        created_date: typing.Optional[dt.datetime] = OMIT,
        last_updated_date: typing.Optional[dt.datetime] = OMIT,
        status: typing.Optional[BaseIntentStatus] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        processor: typing.Optional[PricingAgreementUs50Processor] = OMIT,
        gateway: typing.Optional[GatewayUs50] = OMIT,
        services: typing.Optional[ServicesUs50] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PricingIntent:
        """
        Use this method to create a pricing intent that you can assign to a processing account.

        In the request, you must provide the following:
        -    Processing fees, including the pricing program and the fee to process each transaction.
        -    Gateway fees, including the fee for each transaction handled by our gateway.
        -    Base fees, including maintenance and PCI fees.

        In the response, our gateway returns information about the pricing intent and the pricingIntentId, which you need for the following methods:
        -    [Create Merchant Platform](https://docs.payroc.com/api/schema/boarding/merchant-platforms/create) - Assign the pricing intent to a processing account, when you create the merchant platform and its processing accounts.
        -    [Create Processing Account](https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account) - Assign the pricing intent to a processing account.
        -    [Retrieve Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/retrieve) - Retrieve information about a pricing intent.
        -    [Update Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/update) - Update the details of a pricing intent.
        -    [Delete Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/delete) - Delete a pricing intent.
        -    [Partially Update Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/partially-update) - Partially update the details of a pricing intent.

        Parameters
        ----------
        idempotency_key : str
            Unique identifier that you generate for each request. You must use the [UUID v4 format](https://www.rfc-editor.org/rfc/rfc4122) for the identifier. For more information about the idempotency key, go to [Idempotency](https://docs.payroc.com/api/idempotency).

        key : str
            Unique identifier that you can assign to the pricing intent for your own records.

        country : PricingAgreementUs50Country
            Two-digit code for the country that the pricing intent applies to. The format follows the [ISO-3166-1](https://www.iso.org/iso-3166-country-codes.html) standard.

        version : PricingAgreementUs50Version
            Version of the MPA.

        base : BaseUs

        id : typing.Optional[str]
            Unique identifier of the pricing intent.

        created_date : typing.Optional[dt.datetime]
            Date and time that we received your request to create the pricing intent. We return this value in the [ISO-8601](https://www.iso.org/iso-8601-date-and-time-format.html) format.

        last_updated_date : typing.Optional[dt.datetime]
            Date and time that the pricing intent was last modified. We return this value in the [ISO-8601](https://www.iso.org/iso-8601-date-and-time-format.html) format.

        status : typing.Optional[BaseIntentStatus]
            Status of the pricing intent. The value can be one of the following:
            - `active` - We have approved the pricing intent.
            - `pendingReview` - We have not yet reviewed the pricing intent.
            - `rejected` - We have rejected the pricing intent.

        metadata : typing.Optional[typing.Dict[str, str]]
            [Metadata](https://docs.payroc.com/api/metadata) object that contains your custom data.

        processor : typing.Optional[PricingAgreementUs50Processor]
            Object that contains information about U.S. processor fees.

        gateway : typing.Optional[GatewayUs50]

        services : typing.Optional[ServicesUs50]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PricingIntent
            Successful response. We created the pricing intent and it is waiting for approval.

        Examples
        --------
        from payroc import (
            BaseUs,
            BaseUsAnnualFee,
            BaseUsPlatinumSecurity_Monthly,
            InterchangePlusFees,
            Payroc,
            PricingAgreementUs50Processor,
            PricingAgreementUs50ProcessorCard_InterchangePlus,
            ProcessorFee,
            ServiceUs50_HardwareAdvantagePlan,
        )

        client = Payroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.boarding.pricing_intents.create(
            idempotency_key="8e03978e-40d5-43e8-bc93-6894a57f9324",
            country="US",
            version="5.0",
            base=BaseUs(
                address_verification=5,
                annual_fee=BaseUsAnnualFee(
                    bill_in_month="june",
                    amount=9900,
                ),
                regulatory_assistance_program=15,
                pci_non_compliance=4995,
                merchant_advantage=10,
                platinum_security=BaseUsPlatinumSecurity_Monthly(),
                maintenance=500,
                minimum=100,
                voice_authorization=95,
                chargeback=2500,
                retrieval=1500,
                batch=1500,
                early_termination=57500,
            ),
            processor=PricingAgreementUs50Processor(
                card=PricingAgreementUs50ProcessorCard_InterchangePlus(
                    fees=InterchangePlusFees(
                        mastercard_visa_discover=ProcessorFee(),
                    ),
                ),
            ),
            services=[
                ServiceUs50_HardwareAdvantagePlan(
                    enabled=True,
                )
            ],
            key="Your-Unique-Identifier",
            metadata={"yourCustomField": "abc123"},
        )
        """
        _response = self._raw_client.create(
            idempotency_key=idempotency_key,
            key=key,
            country=country,
            version=version,
            base=base,
            id=id,
            created_date=created_date,
            last_updated_date=last_updated_date,
            status=status,
            metadata=metadata,
            processor=processor,
            gateway=gateway,
            services=services,
            request_options=request_options,
        )
        return _response.data

    def retrieve(
        self, pricing_intent_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PricingIntent:
        """
        Use this method to retrieve information about a pricing intent.

        To retrieve a pricing intent, you need its pricingIntentId. Our gateway returned the pricingIntentId in the response of the [Create Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/create) method.

        **Note:** If you don't have the pricingIntentId, use our [List Pricing Intents](https://docs.payroc.com/api/schema/boarding/pricing-intents/list) method to search for the pricing intent.

        Our gateway returns the following information about the pricing intent:

        - Information about the fees, including the base fees, gateway fees, and processor fees.
        - Status of the pricing intent, including whether we approved the pricing intent.

        Parameters
        ----------
        pricing_intent_id : str
            Unique identifier that we assigned to the pricing intent.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PricingIntent
            Successful response. Returns the requested pricing intent.

        Examples
        --------
        from payroc import Payroc

        client = Payroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.boarding.pricing_intents.retrieve(
            pricing_intent_id="5",
        )
        """
        _response = self._raw_client.retrieve(pricing_intent_id, request_options=request_options)
        return _response.data

    def update(
        self,
        pricing_intent_id: str,
        *,
        key: str,
        country: PricingAgreementUs50Country,
        version: PricingAgreementUs50Version,
        base: BaseUs,
        id: typing.Optional[str] = OMIT,
        created_date: typing.Optional[dt.datetime] = OMIT,
        last_updated_date: typing.Optional[dt.datetime] = OMIT,
        status: typing.Optional[BaseIntentStatus] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        processor: typing.Optional[PricingAgreementUs50Processor] = OMIT,
        gateway: typing.Optional[GatewayUs50] = OMIT,
        services: typing.Optional[ServicesUs50] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Use this method to update the details of a pricing intent. If you update a pricing intent, it won't affect merchant that you've previously onboarded.

        To update a pricing intent, you need its pricingIntentId. Our gateway returned the pricingIntentId in the response of the [Create Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/create) method.

        **Note:** If you don't have the pricingIntentId, use our [List Pricing Intents](https://docs.payroc.com/api/schema/boarding/pricing-intents/list) method to search for the pricing intent.

        You can update the following details about a pricing intent:

        - Fees, including the base fees, processor fees, and gateway fees.
        - Custom name for the pricing intent.
        - Additional services that merchants can sign up for.

        Parameters
        ----------
        pricing_intent_id : str
            Unique identifier that we assigned to the pricing intent.

        key : str
            Unique identifier that you can assign to the pricing intent for your own records.

        country : PricingAgreementUs50Country
            Two-digit code for the country that the pricing intent applies to. The format follows the [ISO-3166-1](https://www.iso.org/iso-3166-country-codes.html) standard.

        version : PricingAgreementUs50Version
            Version of the MPA.

        base : BaseUs

        id : typing.Optional[str]
            Unique identifier of the pricing intent.

        created_date : typing.Optional[dt.datetime]
            Date and time that we received your request to create the pricing intent. We return this value in the [ISO-8601](https://www.iso.org/iso-8601-date-and-time-format.html) format.

        last_updated_date : typing.Optional[dt.datetime]
            Date and time that the pricing intent was last modified. We return this value in the [ISO-8601](https://www.iso.org/iso-8601-date-and-time-format.html) format.

        status : typing.Optional[BaseIntentStatus]
            Status of the pricing intent. The value can be one of the following:
            - `active` - We have approved the pricing intent.
            - `pendingReview` - We have not yet reviewed the pricing intent.
            - `rejected` - We have rejected the pricing intent.

        metadata : typing.Optional[typing.Dict[str, str]]
            [Metadata](https://docs.payroc.com/api/metadata) object that contains your custom data.

        processor : typing.Optional[PricingAgreementUs50Processor]
            Object that contains information about U.S. processor fees.

        gateway : typing.Optional[GatewayUs50]

        services : typing.Optional[ServicesUs50]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from payroc import (
            Ach,
            AchFees,
            BaseUs,
            BaseUsAnnualFee,
            BaseUsPlatinumSecurity_Monthly,
            GatewayUs50,
            GatewayUs50Fees,
            InterchangePlusFees,
            Payroc,
            PricingAgreementUs50Processor,
            PricingAgreementUs50ProcessorCard_InterchangePlus,
            ProcessorFee,
            ServiceUs50_HardwareAdvantagePlan,
        )

        client = Payroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.boarding.pricing_intents.update(
            pricing_intent_id="5",
            country="US",
            version="5.0",
            base=BaseUs(
                address_verification=5,
                annual_fee=BaseUsAnnualFee(
                    bill_in_month="june",
                    amount=9900,
                ),
                regulatory_assistance_program=15,
                pci_non_compliance=4995,
                merchant_advantage=10,
                platinum_security=BaseUsPlatinumSecurity_Monthly(),
                maintenance=500,
                minimum=100,
                voice_authorization=95,
                chargeback=2500,
                retrieval=1500,
                batch=1500,
                early_termination=57500,
            ),
            processor=PricingAgreementUs50Processor(
                card=PricingAgreementUs50ProcessorCard_InterchangePlus(
                    fees=InterchangePlusFees(
                        mastercard_visa_discover=ProcessorFee(),
                    ),
                ),
                ach=Ach(
                    fees=AchFees(
                        transaction=50,
                        batch=5,
                        returns=400,
                        unauthorized_return=1999,
                        statement=800,
                        monthly_minimum=20000,
                        account_verification=10,
                        discount_rate_under_10000=5.25,
                        discount_rate_above_10000=10.0,
                    ),
                ),
            ),
            gateway=GatewayUs50(
                fees=GatewayUs50Fees(
                    monthly=2000,
                    setup=5000,
                    per_transaction=2000,
                    per_device_monthly=10,
                ),
            ),
            services=[
                ServiceUs50_HardwareAdvantagePlan(
                    enabled=True,
                )
            ],
            key="Your-Unique-Identifier",
            metadata={"yourCustomField": "abc123"},
        )
        """
        _response = self._raw_client.update(
            pricing_intent_id,
            key=key,
            country=country,
            version=version,
            base=base,
            id=id,
            created_date=created_date,
            last_updated_date=last_updated_date,
            status=status,
            metadata=metadata,
            processor=processor,
            gateway=gateway,
            services=services,
            request_options=request_options,
        )
        return _response.data

    def delete(self, pricing_intent_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Use this method to delete a pricing intent.

        > **Important:** When you delete a pricing intent, you can't recover it. You also won't be able to assign the pricing intent to a merchant's boarding application.

        To delete a pricing intent, you need its pricingIntentId. Our gateway returned the pricingIntentId in the response of the [Create Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/create) method.

        **Note:** If you don't have the pricingIntentId, use our [List Pricing Intents](https://docs.payroc.com/api/schema/boarding/pricing-intents/list) method to search for the pricing intent.

        Parameters
        ----------
        pricing_intent_id : str
            Unique identifier that we assigned to the pricing intent.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from payroc import Payroc

        client = Payroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.boarding.pricing_intents.delete(
            pricing_intent_id="5",
        )
        """
        _response = self._raw_client.delete(pricing_intent_id, request_options=request_options)
        return _response.data

    def partially_update(
        self,
        pricing_intent_id: str,
        *,
        idempotency_key: str,
        request: PatchRequest,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PricingIntent:
        """
        Use this method to partially update a pricing intent. Structure your request to follow the [RFC 6902](https://datatracker.ietf.org/doc/html/rfc6902) standard.

        If you update a pricing intent, it won't affect merchants you've previously onboarded.

        To update a pricing intent, you need its pricingIntentId. Our gateway returned the pricingIntentId in the response of the [Create Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/create) method.

        **Note:** If you don't have the pricingIntentId, use our [List Pricing Intents](https://docs.payroc.com/api/schema/boarding/pricing-intents/list) method to search for the pricing intent.

        You can update the following details about a pricing intent:

        - Fees, including the base fees, processor fees, and gateway fees.
        - Custom name for the pricing intent.
        - Additional services that merchants can sign up for.

        Parameters
        ----------
        pricing_intent_id : str
            Unique identifier that we assigned to the pricing intent.

        idempotency_key : str
            Unique identifier that you generate for each request. You must use the [UUID v4 format](https://www.rfc-editor.org/rfc/rfc4122) for the identifier. For more information about the idempotency key, go to [Idempotency](https://docs.payroc.com/api/idempotency).

        request : PatchRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PricingIntent
            Successful request. We updated the pricing intent.

        Examples
        --------
        from payroc import PatchDocument_Remove, Payroc

        client = Payroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )
        client.boarding.pricing_intents.partially_update(
            pricing_intent_id="5",
            idempotency_key="8e03978e-40d5-43e8-bc93-6894a57f9324",
            request=[
                PatchDocument_Remove(
                    path="path",
                ),
                PatchDocument_Remove(
                    path="path",
                ),
                PatchDocument_Remove(
                    path="path",
                ),
            ],
        )
        """
        _response = self._raw_client.partially_update(
            pricing_intent_id, idempotency_key=idempotency_key, request=request, request_options=request_options
        )
        return _response.data


class AsyncPricingIntentsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPricingIntentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPricingIntentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPricingIntentsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        before: typing.Optional[str] = None,
        after: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPayrocPager[PricingIntent, PaginatedPricingIntent]:
        """
        Use this method to return a [paginated](https://docs.payroc.com/api/pagination) list of pricing intents associated with the ISV.

        **Note:** If you want to view the details of a specific pricing intent and you have its pricingIntentId, use our [Retrieve Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/retrieve) method.

        Our gateway returns the following information about each pricing intent in the list:

        - Information about the fees, including the base fees, gateway fees, and processor fees.
        - Status of the pricing intent, including whether we approved the pricing intent.

        For each pricing intent, we also return its pricingIntentId which you can use to perform follow-on actions.

        Parameters
        ----------
        before : typing.Optional[str]
            Return the previous page of results before the value that you specify.

            You can’t send the before parameter in the same request as the after parameter.

        after : typing.Optional[str]
            Return the next page of results after the value that you specify.

            You can’t send the after parameter in the same request as the before parameter.

        limit : typing.Optional[int]
            Limit the maximum number of results that we return for each page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPayrocPager[PricingIntent, PaginatedPricingIntent]
            Successful response. Returns a list of pricing intents.

        Examples
        --------
        import asyncio

        from payroc import AsyncPayroc

        client = AsyncPayroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            response = await client.boarding.pricing_intents.list(
                before="2571",
                after="8516",
                limit=1,
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(before=before, after=after, limit=limit, request_options=request_options)

    async def create(
        self,
        *,
        idempotency_key: str,
        key: str,
        country: PricingAgreementUs50Country,
        version: PricingAgreementUs50Version,
        base: BaseUs,
        id: typing.Optional[str] = OMIT,
        created_date: typing.Optional[dt.datetime] = OMIT,
        last_updated_date: typing.Optional[dt.datetime] = OMIT,
        status: typing.Optional[BaseIntentStatus] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        processor: typing.Optional[PricingAgreementUs50Processor] = OMIT,
        gateway: typing.Optional[GatewayUs50] = OMIT,
        services: typing.Optional[ServicesUs50] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PricingIntent:
        """
        Use this method to create a pricing intent that you can assign to a processing account.

        In the request, you must provide the following:
        -    Processing fees, including the pricing program and the fee to process each transaction.
        -    Gateway fees, including the fee for each transaction handled by our gateway.
        -    Base fees, including maintenance and PCI fees.

        In the response, our gateway returns information about the pricing intent and the pricingIntentId, which you need for the following methods:
        -    [Create Merchant Platform](https://docs.payroc.com/api/schema/boarding/merchant-platforms/create) - Assign the pricing intent to a processing account, when you create the merchant platform and its processing accounts.
        -    [Create Processing Account](https://docs.payroc.com/api/schema/boarding/merchant-platforms/create-processing-account) - Assign the pricing intent to a processing account.
        -    [Retrieve Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/retrieve) - Retrieve information about a pricing intent.
        -    [Update Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/update) - Update the details of a pricing intent.
        -    [Delete Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/delete) - Delete a pricing intent.
        -    [Partially Update Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/partially-update) - Partially update the details of a pricing intent.

        Parameters
        ----------
        idempotency_key : str
            Unique identifier that you generate for each request. You must use the [UUID v4 format](https://www.rfc-editor.org/rfc/rfc4122) for the identifier. For more information about the idempotency key, go to [Idempotency](https://docs.payroc.com/api/idempotency).

        key : str
            Unique identifier that you can assign to the pricing intent for your own records.

        country : PricingAgreementUs50Country
            Two-digit code for the country that the pricing intent applies to. The format follows the [ISO-3166-1](https://www.iso.org/iso-3166-country-codes.html) standard.

        version : PricingAgreementUs50Version
            Version of the MPA.

        base : BaseUs

        id : typing.Optional[str]
            Unique identifier of the pricing intent.

        created_date : typing.Optional[dt.datetime]
            Date and time that we received your request to create the pricing intent. We return this value in the [ISO-8601](https://www.iso.org/iso-8601-date-and-time-format.html) format.

        last_updated_date : typing.Optional[dt.datetime]
            Date and time that the pricing intent was last modified. We return this value in the [ISO-8601](https://www.iso.org/iso-8601-date-and-time-format.html) format.

        status : typing.Optional[BaseIntentStatus]
            Status of the pricing intent. The value can be one of the following:
            - `active` - We have approved the pricing intent.
            - `pendingReview` - We have not yet reviewed the pricing intent.
            - `rejected` - We have rejected the pricing intent.

        metadata : typing.Optional[typing.Dict[str, str]]
            [Metadata](https://docs.payroc.com/api/metadata) object that contains your custom data.

        processor : typing.Optional[PricingAgreementUs50Processor]
            Object that contains information about U.S. processor fees.

        gateway : typing.Optional[GatewayUs50]

        services : typing.Optional[ServicesUs50]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PricingIntent
            Successful response. We created the pricing intent and it is waiting for approval.

        Examples
        --------
        import asyncio

        from payroc import (
            AsyncPayroc,
            BaseUs,
            BaseUsAnnualFee,
            BaseUsPlatinumSecurity_Monthly,
            InterchangePlusFees,
            PricingAgreementUs50Processor,
            PricingAgreementUs50ProcessorCard_InterchangePlus,
            ProcessorFee,
            ServiceUs50_HardwareAdvantagePlan,
        )

        client = AsyncPayroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.boarding.pricing_intents.create(
                idempotency_key="8e03978e-40d5-43e8-bc93-6894a57f9324",
                country="US",
                version="5.0",
                base=BaseUs(
                    address_verification=5,
                    annual_fee=BaseUsAnnualFee(
                        bill_in_month="june",
                        amount=9900,
                    ),
                    regulatory_assistance_program=15,
                    pci_non_compliance=4995,
                    merchant_advantage=10,
                    platinum_security=BaseUsPlatinumSecurity_Monthly(),
                    maintenance=500,
                    minimum=100,
                    voice_authorization=95,
                    chargeback=2500,
                    retrieval=1500,
                    batch=1500,
                    early_termination=57500,
                ),
                processor=PricingAgreementUs50Processor(
                    card=PricingAgreementUs50ProcessorCard_InterchangePlus(
                        fees=InterchangePlusFees(
                            mastercard_visa_discover=ProcessorFee(),
                        ),
                    ),
                ),
                services=[
                    ServiceUs50_HardwareAdvantagePlan(
                        enabled=True,
                    )
                ],
                key="Your-Unique-Identifier",
                metadata={"yourCustomField": "abc123"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            idempotency_key=idempotency_key,
            key=key,
            country=country,
            version=version,
            base=base,
            id=id,
            created_date=created_date,
            last_updated_date=last_updated_date,
            status=status,
            metadata=metadata,
            processor=processor,
            gateway=gateway,
            services=services,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(
        self, pricing_intent_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PricingIntent:
        """
        Use this method to retrieve information about a pricing intent.

        To retrieve a pricing intent, you need its pricingIntentId. Our gateway returned the pricingIntentId in the response of the [Create Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/create) method.

        **Note:** If you don't have the pricingIntentId, use our [List Pricing Intents](https://docs.payroc.com/api/schema/boarding/pricing-intents/list) method to search for the pricing intent.

        Our gateway returns the following information about the pricing intent:

        - Information about the fees, including the base fees, gateway fees, and processor fees.
        - Status of the pricing intent, including whether we approved the pricing intent.

        Parameters
        ----------
        pricing_intent_id : str
            Unique identifier that we assigned to the pricing intent.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PricingIntent
            Successful response. Returns the requested pricing intent.

        Examples
        --------
        import asyncio

        from payroc import AsyncPayroc

        client = AsyncPayroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.boarding.pricing_intents.retrieve(
                pricing_intent_id="5",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(pricing_intent_id, request_options=request_options)
        return _response.data

    async def update(
        self,
        pricing_intent_id: str,
        *,
        key: str,
        country: PricingAgreementUs50Country,
        version: PricingAgreementUs50Version,
        base: BaseUs,
        id: typing.Optional[str] = OMIT,
        created_date: typing.Optional[dt.datetime] = OMIT,
        last_updated_date: typing.Optional[dt.datetime] = OMIT,
        status: typing.Optional[BaseIntentStatus] = OMIT,
        metadata: typing.Optional[typing.Dict[str, str]] = OMIT,
        processor: typing.Optional[PricingAgreementUs50Processor] = OMIT,
        gateway: typing.Optional[GatewayUs50] = OMIT,
        services: typing.Optional[ServicesUs50] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Use this method to update the details of a pricing intent. If you update a pricing intent, it won't affect merchant that you've previously onboarded.

        To update a pricing intent, you need its pricingIntentId. Our gateway returned the pricingIntentId in the response of the [Create Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/create) method.

        **Note:** If you don't have the pricingIntentId, use our [List Pricing Intents](https://docs.payroc.com/api/schema/boarding/pricing-intents/list) method to search for the pricing intent.

        You can update the following details about a pricing intent:

        - Fees, including the base fees, processor fees, and gateway fees.
        - Custom name for the pricing intent.
        - Additional services that merchants can sign up for.

        Parameters
        ----------
        pricing_intent_id : str
            Unique identifier that we assigned to the pricing intent.

        key : str
            Unique identifier that you can assign to the pricing intent for your own records.

        country : PricingAgreementUs50Country
            Two-digit code for the country that the pricing intent applies to. The format follows the [ISO-3166-1](https://www.iso.org/iso-3166-country-codes.html) standard.

        version : PricingAgreementUs50Version
            Version of the MPA.

        base : BaseUs

        id : typing.Optional[str]
            Unique identifier of the pricing intent.

        created_date : typing.Optional[dt.datetime]
            Date and time that we received your request to create the pricing intent. We return this value in the [ISO-8601](https://www.iso.org/iso-8601-date-and-time-format.html) format.

        last_updated_date : typing.Optional[dt.datetime]
            Date and time that the pricing intent was last modified. We return this value in the [ISO-8601](https://www.iso.org/iso-8601-date-and-time-format.html) format.

        status : typing.Optional[BaseIntentStatus]
            Status of the pricing intent. The value can be one of the following:
            - `active` - We have approved the pricing intent.
            - `pendingReview` - We have not yet reviewed the pricing intent.
            - `rejected` - We have rejected the pricing intent.

        metadata : typing.Optional[typing.Dict[str, str]]
            [Metadata](https://docs.payroc.com/api/metadata) object that contains your custom data.

        processor : typing.Optional[PricingAgreementUs50Processor]
            Object that contains information about U.S. processor fees.

        gateway : typing.Optional[GatewayUs50]

        services : typing.Optional[ServicesUs50]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from payroc import (
            Ach,
            AchFees,
            AsyncPayroc,
            BaseUs,
            BaseUsAnnualFee,
            BaseUsPlatinumSecurity_Monthly,
            GatewayUs50,
            GatewayUs50Fees,
            InterchangePlusFees,
            PricingAgreementUs50Processor,
            PricingAgreementUs50ProcessorCard_InterchangePlus,
            ProcessorFee,
            ServiceUs50_HardwareAdvantagePlan,
        )

        client = AsyncPayroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.boarding.pricing_intents.update(
                pricing_intent_id="5",
                country="US",
                version="5.0",
                base=BaseUs(
                    address_verification=5,
                    annual_fee=BaseUsAnnualFee(
                        bill_in_month="june",
                        amount=9900,
                    ),
                    regulatory_assistance_program=15,
                    pci_non_compliance=4995,
                    merchant_advantage=10,
                    platinum_security=BaseUsPlatinumSecurity_Monthly(),
                    maintenance=500,
                    minimum=100,
                    voice_authorization=95,
                    chargeback=2500,
                    retrieval=1500,
                    batch=1500,
                    early_termination=57500,
                ),
                processor=PricingAgreementUs50Processor(
                    card=PricingAgreementUs50ProcessorCard_InterchangePlus(
                        fees=InterchangePlusFees(
                            mastercard_visa_discover=ProcessorFee(),
                        ),
                    ),
                    ach=Ach(
                        fees=AchFees(
                            transaction=50,
                            batch=5,
                            returns=400,
                            unauthorized_return=1999,
                            statement=800,
                            monthly_minimum=20000,
                            account_verification=10,
                            discount_rate_under_10000=5.25,
                            discount_rate_above_10000=10.0,
                        ),
                    ),
                ),
                gateway=GatewayUs50(
                    fees=GatewayUs50Fees(
                        monthly=2000,
                        setup=5000,
                        per_transaction=2000,
                        per_device_monthly=10,
                    ),
                ),
                services=[
                    ServiceUs50_HardwareAdvantagePlan(
                        enabled=True,
                    )
                ],
                key="Your-Unique-Identifier",
                metadata={"yourCustomField": "abc123"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            pricing_intent_id,
            key=key,
            country=country,
            version=version,
            base=base,
            id=id,
            created_date=created_date,
            last_updated_date=last_updated_date,
            status=status,
            metadata=metadata,
            processor=processor,
            gateway=gateway,
            services=services,
            request_options=request_options,
        )
        return _response.data

    async def delete(self, pricing_intent_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Use this method to delete a pricing intent.

        > **Important:** When you delete a pricing intent, you can't recover it. You also won't be able to assign the pricing intent to a merchant's boarding application.

        To delete a pricing intent, you need its pricingIntentId. Our gateway returned the pricingIntentId in the response of the [Create Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/create) method.

        **Note:** If you don't have the pricingIntentId, use our [List Pricing Intents](https://docs.payroc.com/api/schema/boarding/pricing-intents/list) method to search for the pricing intent.

        Parameters
        ----------
        pricing_intent_id : str
            Unique identifier that we assigned to the pricing intent.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from payroc import AsyncPayroc

        client = AsyncPayroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.boarding.pricing_intents.delete(
                pricing_intent_id="5",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(pricing_intent_id, request_options=request_options)
        return _response.data

    async def partially_update(
        self,
        pricing_intent_id: str,
        *,
        idempotency_key: str,
        request: PatchRequest,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PricingIntent:
        """
        Use this method to partially update a pricing intent. Structure your request to follow the [RFC 6902](https://datatracker.ietf.org/doc/html/rfc6902) standard.

        If you update a pricing intent, it won't affect merchants you've previously onboarded.

        To update a pricing intent, you need its pricingIntentId. Our gateway returned the pricingIntentId in the response of the [Create Pricing Intent](https://docs.payroc.com/api/schema/boarding/pricing-intents/create) method.

        **Note:** If you don't have the pricingIntentId, use our [List Pricing Intents](https://docs.payroc.com/api/schema/boarding/pricing-intents/list) method to search for the pricing intent.

        You can update the following details about a pricing intent:

        - Fees, including the base fees, processor fees, and gateway fees.
        - Custom name for the pricing intent.
        - Additional services that merchants can sign up for.

        Parameters
        ----------
        pricing_intent_id : str
            Unique identifier that we assigned to the pricing intent.

        idempotency_key : str
            Unique identifier that you generate for each request. You must use the [UUID v4 format](https://www.rfc-editor.org/rfc/rfc4122) for the identifier. For more information about the idempotency key, go to [Idempotency](https://docs.payroc.com/api/idempotency).

        request : PatchRequest

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PricingIntent
            Successful request. We updated the pricing intent.

        Examples
        --------
        import asyncio

        from payroc import AsyncPayroc, PatchDocument_Remove

        client = AsyncPayroc(
            client_id="YOUR_CLIENT_ID",
            client_secret="YOUR_CLIENT_SECRET",
        )


        async def main() -> None:
            await client.boarding.pricing_intents.partially_update(
                pricing_intent_id="5",
                idempotency_key="8e03978e-40d5-43e8-bc93-6894a57f9324",
                request=[
                    PatchDocument_Remove(
                        path="path",
                    ),
                    PatchDocument_Remove(
                        path="path",
                    ),
                    PatchDocument_Remove(
                        path="path",
                    ),
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.partially_update(
            pricing_intent_id, idempotency_key=idempotency_key, request=request, request_options=request_options
        )
        return _response.data
