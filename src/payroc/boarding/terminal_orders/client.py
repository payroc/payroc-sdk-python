# This file was auto-generated by Fern from our API Definition.

import typing

from ...core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ...core.request_options import RequestOptions
from ...types.terminal_order import TerminalOrder
from .raw_client import AsyncRawTerminalOrdersClient, RawTerminalOrdersClient


class TerminalOrdersClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawTerminalOrdersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawTerminalOrdersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawTerminalOrdersClient
        """
        return self._raw_client

    def retrieve(
        self, terminal_order_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> TerminalOrder:
        """
        Use this method to retrieve information about a terminal order.

        To retrieve a terminal order, you need it's terminalOrderId. Our gateway returned the terminalOrderId in the response of the [Create Terminal Order](https://docs.payroc.com/api/schema/boarding/processing-accounts/create-terminal-order) method.

        **Note**: If you don't have the terminalOrderId, use our [List Terminal Orders](https://docs.payroc.com/api/schema/boarding/processing-accounts/list-terminal-orders) method to search for the terminal order.

        Our gateway returns the following information about the terminal order:
        - Status of the order
        - Items in the order
        - Training provider
        - Shipping information

        **Note**: You can subscribe to our terminalOrder.status.changed event to get notifications when we update the status of a terminal order. For more information about how to subscribe to events, go to [Events Subscriptions](https://docs.payroc.com/guides/board-merchants/event-subscriptions).

        Parameters
        ----------
        terminal_order_id : str
            Unique identifier of the terminal order.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TerminalOrder
            Successful request. Returns the terminal order.

        Examples
        --------
        from payroc import Payroc

        client = Payroc(
            api_key="YOUR_API_KEY",
        )
        client.boarding.terminal_orders.retrieve(
            terminal_order_id="12345",
        )
        """
        _response = self._raw_client.retrieve(terminal_order_id, request_options=request_options)
        return _response.data


class AsyncTerminalOrdersClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawTerminalOrdersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawTerminalOrdersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawTerminalOrdersClient
        """
        return self._raw_client

    async def retrieve(
        self, terminal_order_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> TerminalOrder:
        """
        Use this method to retrieve information about a terminal order.

        To retrieve a terminal order, you need it's terminalOrderId. Our gateway returned the terminalOrderId in the response of the [Create Terminal Order](https://docs.payroc.com/api/schema/boarding/processing-accounts/create-terminal-order) method.

        **Note**: If you don't have the terminalOrderId, use our [List Terminal Orders](https://docs.payroc.com/api/schema/boarding/processing-accounts/list-terminal-orders) method to search for the terminal order.

        Our gateway returns the following information about the terminal order:
        - Status of the order
        - Items in the order
        - Training provider
        - Shipping information

        **Note**: You can subscribe to our terminalOrder.status.changed event to get notifications when we update the status of a terminal order. For more information about how to subscribe to events, go to [Events Subscriptions](https://docs.payroc.com/guides/board-merchants/event-subscriptions).

        Parameters
        ----------
        terminal_order_id : str
            Unique identifier of the terminal order.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TerminalOrder
            Successful request. Returns the terminal order.

        Examples
        --------
        import asyncio

        from payroc import AsyncPayroc

        client = AsyncPayroc(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.boarding.terminal_orders.retrieve(
                terminal_order_id="12345",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(terminal_order_id, request_options=request_options)
        return _response.data
